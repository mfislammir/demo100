git config --global user.name "mfislammir"
git config --global user.email "mfislammir@gmail.com:"

git init
git add .
git status
git commit -m"Something relavent to changes"
LM local machine
rm remote machine (github)


echo "# demo100" >> README.md
git init
git add README.md
git commit -m"first commit"
git branch -M main
git remote add origin https://github.com/mfislammir/demo100.git
git push -u origin main
git pull origin main
git close url


.idea/
/*.iml
git stach command to ignore own changes/not deleted

git branch
git checkout -b fokrul
git checkput main to go back to main branceh